packages:
  - web
  - server

# 允许运行 postinstall 脚本的依赖（pnpm v10+ 默认不运行）
onlyBuiltDependencies:
  - electron
  - electron-builder
  - better-sqlite3
  - esbuild

# 使用扁平 node_modules 布局，兼容 electron-builder 原生模块打包
node-linker: hoisted
